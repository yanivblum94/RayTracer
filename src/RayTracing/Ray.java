package RayTracing;

public class Ray {
    public Vector Direction;
    public Vector Origin;

    public Ray(Vector direction, Vector origin) {
        Direction = direction;
        Origin = origin;
    }
}
